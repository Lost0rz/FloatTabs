name: Stage 0 macOS (retired)

# Historical Stage 0 workflow kept only as an explicit manual fallback.
# The maintained baseline workflow is .github/workflows/macos-ci.yml.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: macos-26
    steps:
      - run: echo 'Stage 0 workflow is retired; use macOS CI.'
