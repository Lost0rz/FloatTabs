name: QA DMG

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'FloatTabs/**'
      - 'FloatTabs.xcodeproj/**'
      - 'tools/release/**'
      - '.github/workflows/qa-dmg.yml'

jobs:
  build-dmg:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v5

      - name: Build and verify Universal 2 QA DMG
        env:
          FLOATTABS_OUTPUT_DIR: ${{ runner.temp }}/FloatTabsRelease
        run: |
          chmod +x tools/release/build_dmg.sh
          tools/release/build_dmg.sh

      # Temporary QA branch only: retain the binary for this pull-request build
      # so the current unmerged application tree can be tested locally.
      - name: Upload manually requested Universal 2 QA DMG and debug symbols
        if: github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: FloatTabs-0.1.1-Universal-QA-DMG
          if-no-files-found: error
          path: |
            ${{ runner.temp }}/FloatTabsRelease/FloatTabs-*.dmg
            ${{ runner.temp }}/FloatTabsRelease/FloatTabs-*.dmg.sha256
            ${{ runner.temp }}/FloatTabsRelease/FloatTabs-*.dSYM.zip
            ${{ runner.temp }}/FloatTabsRelease/FloatTabs-*.dSYM.zip.sha256
