# FloatTabs v0.1.3 Build 5 correction trigger

This temporary marker exists only to trigger the validated Settings version/build correction workflow after this PR is merged.

The workflow removes this file together with itself when it commits the validated product patch to `main`.
